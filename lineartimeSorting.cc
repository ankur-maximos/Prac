#include<iostream>
#include<vector>
#include<math.h>
using namespace std;

countingSort(vector<int>) {

}

int main() {
    freopen("input.txt","r",stdin); 
    int n;
    cin>> n;
    vector<int> sort;
    for(int i=0;i<n;i++) {
        cin>>num;
        cout<<isPrime(num);
    }  
    return 0;
}
